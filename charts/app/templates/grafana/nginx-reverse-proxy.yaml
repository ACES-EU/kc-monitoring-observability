apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.nginxReverseProxy.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.nginxReverseProxy.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.nginxReverseProxy.name }}
    spec:
      containers:
        - name: nginx
          image: {{ .Values.nginxReverseProxy.image }}
          ports:
            - containerPort: {{ .Values.nginxReverseProxy.service.targetPort }}
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx
      volumes:
        - name: nginx-config
          configMap:
            name: {{ .Values.nginxReverseProxy.configMapName }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nginxReverseProxy.name }}
spec:
  selector:
    app: {{ .Values.nginxReverseProxy.name }}
  type: NodePort
  ports:
    - protocol: TCP
      port: {{ .Values.nginxReverseProxy.service.port }}
      targetPort: {{ .Values.nginxReverseProxy.service.targetPort }}
      nodePort: {{ .Values.nginxReverseProxy.service.nodePort }}

