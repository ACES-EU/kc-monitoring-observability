{{- if .Values.grafanaNginxReverseProxy.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  name: {{ .Values.grafanaNginxReverseProxy.name }}-ingress
  labels:
    app: {{ .Values.grafanaNginxReverseProxy.name }}
spec:
  {{- if .Values.grafanaNginxReverseProxy.ingress.className }}
  ingressClassName: {{ .Values.grafanaNginxReverseProxy.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.grafanaNginxReverseProxy.ingress.host }}
      http:
        paths:
          - path: {{ .Values.grafanaNginxReverseProxy.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.grafanaNginxReverseProxy.name }}
                port:
                  number: {{ .Values.grafanaNginxReverseProxy.service.port }}
  {{- if .Values.grafanaNginxReverseProxy.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.grafanaNginxReverseProxy.ingress.host }}
      secretName: {{ .Values.grafanaNginxReverseProxy.ingress.tlsSecretName }}
  {{- end }}
{{- end }}
