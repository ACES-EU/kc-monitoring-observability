# NATS configuration
nats.server.url=${NATS_URL:nats://localhost:4222}

# Define the time window for aggregation in milliseconds
aggregation.window.ms=60000

# NATS subject to publish the aggregation result
aggregation.result.subject=aggregated-topic
